const state = {
  items: [{}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}],
  bidders: [{ name: 'Bidder #1' }, { name: 'Bidder #2' }]
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
